# AstrBot 插件自主开发巡逻记录 - 2026-05-24 3h

## 巡逻范围

- 执行类型：每 3 小时 GitHub MCP 我方 AstrBot 插件自主开发巡逻
- 时间：2026-05-24
- 明确排除：未巡逻、未操作 `SXP-Simon/astrbot_plugin_qq_group_daily_analysis`

## 仓库盘点

本轮搜索 `Liangyu-G` 下 AstrBot 插件相关仓库，优先 `astrbot_plugin_*`，盘点到：

1. `Liangyu-G/astrbot_plugin_points_mall`
2. `Liangyu-G/astrbot_plugin_ops_health`
3. `Liangyu-G/astrbot-plugin-patrol-notes`

## 本轮目标

选择上轮提到的 `Liangyu-G/astrbot_plugin_points_mall` 作为小维护目标，执行只读检查与巡逻记录沉淀。

## 检查结果

### `.gitignore`

当前内容：

```gitignore
__pycache__/
*.pyc
*.json
*.json.bak
```

结论：

- 已覆盖 `__pycache__/` 和 `*.pyc`。
- 已覆盖运行时 JSON 数据和备份文件。
- 但仓库根目录存在需要跟踪的 `_conf_schema.json`，当前 `*.json` 规则较宽，后续新增 JSON 文档或配置样例时容易被误忽略。

建议后续收窄为运行时数据专用规则，例如：

```gitignore
__pycache__/
*.py[cod]

# Runtime data generated by the plugin
points_data*.json
points_stock_item_*.json
*.json.bak
*.tmp
```

如需继续忽略插件数据目录，也可补充：

```gitignore
data/plugins/astrbot_plugin_points_mall/
```

### `__pycache__`

仓库中仍跟踪了：

- `__pycache__/main.cpython-312.pyc`

结论：

- `.gitignore` 只能阻止后续新增，不能自动移除已跟踪文件。
- 建议后续用 `git rm --cached -r __pycache__` 移除已跟踪 pyc，再提交。
- 本轮未删除文件、未强推、未发布 release。

### README / main.py / _conf_schema.json 一致性

核对到以下配置项在 README 与 `_conf_schema.json` 中一致出现，且 `main.py` 有对应读取逻辑：

- `super_admins`
- `admins`
- `sign_min`
- `sign_max`
- `active_text_len`
- `active_point_reward`
- `active_cooldown`
- `low_stock_warn_threshold`
- `sold_stock_retention_days`
- `use_t2i`

核对到主要命令在 README 与 `main.py` 中一致出现，包括：

- 用户命令：`/积分帮助`、`/签到`、`/我的积分`、`/积分排行`、`/积分商场`、`/购买`
- 管理员命令：`/上架`、`/设置发货`、`/添加库存`、`/批量添加库存`、`/查看库存`、`/库存明细`、`/导出库存`、`/清空库存`、`/清理已售库存`、`/待审核商品`、`/下架`、`/订单列表`、`/查订单`
- 超级管理员命令：`/授权上架`、`/给予积分`、`/强制退款`、`/商城自检`、`/积分流水`、`/设置已售保留天数`、`/清理到期已售库存`

备注：README 与代码中均使用“积分商场”命令名；插件定位/仓库名为“积分商城”。这是可接受的历史命名差异，但如需降低用户混淆，后续可增加 `/积分商城` 作为别名，并更新 README。

## 可审计产出

本轮原计划在 `Liangyu-G/astrbot_plugin_points_mall` 创建维护 issue，但 GitHub API 返回 `Validation Failed`，疑似该仓库 issue 功能不可用或字段校验受限。因此改为在巡逻记录仓库沉淀本文档，形成可审计文档更新。

## 风险与回滚建议

- 本轮未修改 `astrbot_plugin_points_mall` 业务代码，未删除仓库文件，运行风险低。
- 本文档仅为巡逻记录，如需回滚，可 revert 本次文档提交。
- 后续若删除 pycache，应只移除版本控制中的构建产物；如误删，可从提交历史恢复，但 pyc 通常不应纳入仓库。
- 后续若调整 `.gitignore`，应注意保留 `_conf_schema.json` 的可跟踪状态。

## 下一轮计划

1. 优先尝试在 `astrbot_plugin_points_mall` 建立维护分支，完成：
   - 移除已跟踪 `__pycache__/main.cpython-312.pyc`
   - 收窄 `.gitignore` JSON 忽略规则
2. 视权限与仓库设置创建 PR；如仍无法写入目标仓库，则继续在巡逻记录仓库追加 issue/文档。
3. 可选增强：为 `/积分商场` 增加 `/积分商城` 别名，并同步 README。
